# Slice 28 — Final match on Cloudflare

The user requested committing, pushing and deploying the final result. Source `911be81` was already committed and present on `origin/main`; `git push origin main` confirmed it was up to date. The existing Cloudflare Workers Static Assets site now serves **Two minutes, two models** as its only bundled recording. Version `2be72a68-3f7c-4380-9224-252230cacacb` is live at <https://llm-football.lore-cinque.workers.dev>.

The recording remains `final-match-001`, `football-0.9`, Coral 0–5 Cyan, exactly 120 playing seconds and independent replay hash `fae9b050`. The first and only final run is preserved. No model calls, football changes or presentation changes were made during release. The main README and analytics snippet remain unchanged.

The source was clean and unchanged from the successful TypeScript, 287-test, production-build, stream-import and replay checks in slice 27. Deployment reused that exact build. Its catalogue and compressed match were compared directly with the verified public files before upload. The Cloudflare account check and deployment dry run passed. All eight served files match the saved build by SHA-256; eight older recording URLs, including Playing to win, return 404.

Live browser checks passed: playback starts and advances, pause works, and seeking to the final frame shows Coral 0–5 Cyan at **02:00 playing time**. The watch timeline is **192.5 seconds**, displayed as **03:12**, because it includes stoppages and celebrations and the clock displays whole seconds. The Matches inspector shows the correct title and model labels, with only the final recording and no development practice options. Browser logs contain no warnings or errors. The live tab was reloaded and left ready to watch. Analytics dashboard receipt remains unchecked.

Wrangler uploads only `dist/`. The catalogue names the compressed recording; the browser decompresses and validates it, then replays recorded decisions using the bundled engine. To verify a release, recursively hash each built file, request each corresponding live URL with identity transfer encoding, and compare the response bytes and status. Checking retired recording URLs separately confirms they were removed from the release. Keeping the viewer and match in one verified build preserves the current ruleset boundary. Private generation artifacts and provider credentials stay outside the published directory. Build snapshot, manifest and live verification report are retained under ignored `artifacts/private/final-match-release/`.

Next: prepare accurate X and LinkedIn posts and recorded highlights from this actual match. This single, one-sided result does not establish general model performance. Ordinary football mistakes remain part of the recording; no further paid run is planned.
